/*
 * This is a shared library function intended to be used during the unit test stage
 * of a pipeline.  Unit tests might differ depending on the application. 
 *
 * @param buildType specifies the type of application.
 */

def call(String buildType) {

  if (buildType == "maven"){

  }
  else if (buildType == "nodejs"){

  }
}
