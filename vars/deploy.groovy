/*
 * Deploys a project to an environment.
 *
 * @param buildType specifies the type of application.
 * @param config is meant to be a map of key-value pairs that would be needed to connect to an environment.
 */

def call(String buildType, config) {
  if (buildType == "maven"){ 

  }
  else if (buildType == "nodejs"){

  }
}
