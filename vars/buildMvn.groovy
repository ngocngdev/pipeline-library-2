/*
 * This is a shared library function intended to be used during the build stage
 * of a pipeline. This is intended to be used for Maven builds.
 *
 */

def call() {

/*
 * Example:
 * sh 'mvn clean install -DskipTests=true -B'
 */

}
